// Placeholder — timers, docket grid, and rate resolution land in Phase 5
// (docs/PLAN.md), on top of packages/db/src/schema/time.ts.
export default function TimePage() {
  return (
    <div>
      <h1 className="mb-4 text-xl font-semibold">Time</h1>
      <p className="text-sm text-zinc-500">Not yet implemented — see docs/PLAN.md Phase 5.</p>
    </div>
  );
}
